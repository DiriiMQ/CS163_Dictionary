#include "Window.h"
#include <iostream>
#include "_raygui.h"
#include <core/Dict.h>

int main() {
    Dict dict = Dict();
   int n = dict.words.size();
   cout << n;
   /* Window  window;
    if (n > 0)
    window.run();*/
    
    return 0;
}
